#!/usr/bin/env bash

set -e

config_dir=$HOME/.config/nvim

echo -n "generating help tags " >&2

for dir in "$config_dir"/doc "$config_dir"/pack/default/start/*/doc; do
    vim -c "helptags $dir" -c q &>/dev/null
    echo -n "." >&2
done

echo " done" >&2

#!/usr/bin/env bash

# List the submodule paths in this repository.

set -eu

config_file=$PWD/.gitmodules

[[ -f $config_file ]] || exit 0

git config --file "$config_file" --name-only --get-regex "\.path$" |
    xargs -r -I{} git config --file "$config_file" --get "{}"

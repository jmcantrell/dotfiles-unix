#!/usr/bin/env bash

# Execute a command in a given directory.
# Usage: ind DIRECTORY COMMAND [ARGUMENT...]

set -euo pipefail

dir=${1:?missing directory}

shift

if (($# == 0)); then
    printf "The second argument must be a command\n" >&2
    exit 1
fi

cd "$dir"
exec "$@"

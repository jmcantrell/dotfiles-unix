#!/usr/bin/env sh

# Start an interactive bash that's recorded to a file.
# Usage: script-bash [OPTIONS] [FILE]
# See script(1) for options.

PS1='\$ ' script -q -c 'bash --login --noprofile --norc' "$@"

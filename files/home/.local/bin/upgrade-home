#!/usr/bin/env bash

set -euo pipefail

enable-rustup
enable-ghcup

enable-python -d
enable-ruby -d
enable-node -d
enable-deno -d
enable-lua -d

enable-secrets

cd ~/.dotfiles
./scripts/upgrade
./scripts/deploy

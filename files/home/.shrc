for dir in ~/.shrc.d ~/.shrc-"${HOST%.local}".d; do
    test -d "$dir" || continue
    for file in "$dir"/*; do
        test -r "$file" || continue
        . "$file" || echo "ERROR: Unable to load: $file" >&2
    done
done
unset dir file

# vi:ft=sh

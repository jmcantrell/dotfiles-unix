alias v='vim'

# Edit files as root.
alias V='sudo -e'

# Edit program(s) in $PATH.
vw() {
    command which "$@" 2>/dev/null | vin
}

# Edit the output of a command.
vx() {
    "$@" | vim -
}

# Edit all output of a command.
vX() {
    "$@" 2>&1 | vim -
}

# vi:ft=sh

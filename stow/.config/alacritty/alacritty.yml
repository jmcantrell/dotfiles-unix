import:
  - ~/.config/alacritty/local.yml
  - ~/.config/alacritty/colors.yml

env:
  TERM: xterm-256color

selection:
  save_to_clipboard: true

scrolling:
  history: 100000

cursor:
  style:
    blinking: Always

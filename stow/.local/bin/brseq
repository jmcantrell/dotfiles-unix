#!/usr/bin/env bash

set -e

# Default options:
format="%d/%b (%n)%e"
start_num=1
increment=1
unset width noop

me=${0##*/}
usage="Rename files as a sequence of numbers.

Usage: $me [-h] [-n] [-w] [-s NUMBER] [-i NUMBER] [-f FORMAT] FILE...

Options:
    -h           show this help message
    -w           equalize width by padding with leading zeroes
    -n           show the renamed files without actually renaming them
    -s NUMBER    change start number (default: 1)
    -i NUMBER    change increment (default: 1)
    -f FORMAT    format file basename before extension (default: '$format')
"

usage() {
    echo "$usage" >&2
    exit 0
}

die() {
    echo "$me: $*" >&2
    exit 1
}

declare -a seq_options

unset OPTIND OPTARG
while getopts ":hf:s:i:W:wn" option; do
    case $option in
    h) usage ;;

    f) format=$OPTARG ;;
    s) start_num=$OPTARG ;;
    i) increment=$OPTARG ;;
    W) width=$OPTARG ;;
    w) seq_options+=(-w) ;;
    n) noop=1 ;;

    *) die "invalid option '$option'" ;;
    esac
done && shift $((OPTIND - 1))

[[ $format == *%n* ]] || die "format does not contain '%n'"
[[ $width == *[!0-9]* ]] && die "width must be a positive integer"
[[ $start_num == *[!0-9]* ]] && die "start number must be a positive integer"
[[ $increment == *[!0-9]* ]] && die "increment must be a positive integer"

end_num=$((start_num + $# - 1))

while read -r num; do
    [[ $width ]] && num=$(printf "%0${width}d" "$num")

    src=$1
    shift

    dir=$(dirname "$src")
    ext=$(extname "$src")
    base=$(basename "$src" "$ext")

    dst=$format
    dst=${dst/\%d/$dir}
    dst=${dst/\%b/$base}
    dst=${dst/\%n/$num}
    dst=${dst/\%e/$ext}

    [[ $noop ]] || mv -i -T "$src" "$dst"

    echo "$dst"
done < <(seq "${seq_options[@]}" "$start_num" "$increment" "$end_num")

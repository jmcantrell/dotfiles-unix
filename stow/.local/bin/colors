#!/usr/bin/env bash

# Create fg and bg color value arrays to be sourced.

set -e

# already sourced
[[ $reset ]] && exit 0

reset=$(tput sgr0)

declare -a fg bg
for n in {0..15}; do
    fg+=("$(tput setaf "$n")")
    bg+=("$(tput setab "$n")")
done

#!/usr/bin/env sh

# Install global python packages.

set -e

pip_install() {
    pip install --user --upgrade --upgrade-strategy only-if-needed "$@"
}

pip_install pip
pip_install pipenv wheel poetry
pip_install black bpython pudb i3ipc jedi-language-server neovim

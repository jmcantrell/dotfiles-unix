#!/usr/bin/env bash

# Install global python packages.

set -e

pip_install() {
    pip install --user --force-reinstall "$@"
}

pip_install pip
pip_install pipenv wheel poetry black bpython pudb i3ipc jedi-language-server neovim

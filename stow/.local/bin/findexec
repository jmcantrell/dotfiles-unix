#!/usr/bin/env sh

# Find files and pass them to a command.
# Usage: findexec dir pattern [command]

dir=${1:?no directory given}
pattern=${2:?no pattern given}

exec find "$dir" \
    -type f \
    -iname "*$pattern*" \
    -exec "$@" '{}' \;

#!/usr/bin/env bash

# Repair permissions on an ssh directory.
# Usage: fixssh [DIRECTORY]

set -euo pipefail

dir=${1:-$HOME/.ssh}

chmod go-w "$(dirname "$dir")"
chmod 700 "$dir"
chmod 600 "$dir"/{id_*,*.pub}

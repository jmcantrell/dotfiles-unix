#!/usr/bin/env bash

# Show the root directory for this git repository.

set -e

dir=${1:-$PWD}

# check each parent for the git directory
while [[ -d $dir && ! -d $dir/.git ]]; do
    dir+=/..
done

[[ -d $dir ]] && echo "$dir"

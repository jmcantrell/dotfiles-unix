#!/usr/bin/env bash

# Show the root git directory.

set -eu

dir=${1:-$PWD}

if [[ ! -d $dir ]]; then
    dir=$(dirname "$dir")
fi

while [[ -d $dir && ! -e $dir/.git ]]; do
    dir+=/..
done

if [[ -d $dir ]]; then
    realpath "$dir"
fi

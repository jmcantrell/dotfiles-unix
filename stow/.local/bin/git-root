#!/usr/bin/env bash

# Show the root git directory.

set -eu

dir=${1:-$PWD}

[[ -d $dir ]] || dir=$(dirname "$dir")

while [[ -d $dir && ! -e $dir/.git ]]; do
    dir+=/..
done

[[ -d $dir ]] && realpath "$dir"

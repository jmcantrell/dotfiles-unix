#!/usr/bin/env bash

# Show the root git directory.

set -euo pipefail

dir=${1:-$PWD}

[[ -d $dir ]] || dir=$(dirname "$dir")

while [[ -d $dir && ! -d $dir/.git ]]; do
    dir+=/..
done

[[ -d $dir ]] && realpath "$dir"

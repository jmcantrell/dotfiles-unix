#!/usr/bin/env bash

# Show the nearest git directory.

set -euo pipefail

dir=${1:-$PWD}

[[ -d $dir ]] || dir=$(dirname "$dir")

while [[ -d $dir && ! -e $dir/.git ]]; do
    dir+=/..
done

[[ -d $dir ]] && realpath "$dir"

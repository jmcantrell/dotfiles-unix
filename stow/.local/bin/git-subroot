#!/usr/bin/env bash

# Show the nearest git directory.

set -e

dir=${1:-$PWD}

# In case the command target is a file.
[[ -d $dir ]] || dir=$(dirname "$dir")

# check each parent for the git directory
while [[ -d $dir && ! -e $dir/.git ]]; do
    dir+=/..
done

[[ -d $dir ]] && realpath "$dir"

#!/usr/bin/env bash

# Execute a command in a given directory.
# usage: ind directory command [arg...]

set -e

dir=${1:?no directory given}
shift

[[ -d $dir ]] || exit 1
cd "$dir" || exit 1
pwd >&2
exec "$@"

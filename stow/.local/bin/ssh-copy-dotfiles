#!/usr/bin/env bash

set -eu

host=$1

rsync -avz --delete --delete-excluded \
    --cvs-exclude --exclude=.git --exclude=.gitmodules \
    ~/.dotfiles "$host":~

ssh "$host" "cd ~/.dotfiles && ./install.sh && ./config.sh && ./stow.sh"

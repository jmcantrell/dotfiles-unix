#!/usr/bin/env sh

# Verify files for the given checksum files.
# Usage: sumcheck [file...]

set -e

for sumfile in "$@"; do
    alg=${sumfile##*.}
    case $alg in
        md5|sha*)
            "${alg}sum" -c "$sumfile"
            ;;
    esac
done

#!/usr/bin/env bash

# Edit any files passed over stdin.
# Example: find . -type f | vim-stdin

set -eu

readarray -t paths

if ((${#paths[@]} == 0)); then
    echo "ERROR: Nothing to edit" >&2
    exit 1
fi

exec vim "${paths[@]}"

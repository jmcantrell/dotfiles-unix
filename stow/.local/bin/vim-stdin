#!/usr/bin/env bash

# Edit any files passed over stdin.

set -euo pipefail

readarray -t paths

if ((${#paths[@]} == 0)); then
    echo "${0##*/}: nothing to edit" >&2
    exit 1
fi

exec vim "${paths[@]}"

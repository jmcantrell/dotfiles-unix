load_sh() {
    for file in "$@"; do
        if test -f "$file"; then
            . "$file" || echo "unable to load: $file" >&2
        fi
    done
    unset file
}

load_sh ~/.shrc.d/*

unset -f load_sh

# vi:ft=sh

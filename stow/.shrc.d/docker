alias d='docker'
alias dc='docker-compose'

drun() {
    docker run -it --rm "$@"
}

dcup() {
    docker-compose up --detach --build "$@"
}

dcdown() {
    docker-compose down --remove-orphans "$@"
}

# vi:ft=sh

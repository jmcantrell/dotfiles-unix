for locate in plocate mlocate locate; do
    if which "$locate" >/dev/null; then
        export LOCATE=$locate
    fi
done

if [[ $LOCATE ]]; then
    alias locate='$LOCATE -i'
    alias updatedb='sudo updatedb'
fi

# vi:ft=sh

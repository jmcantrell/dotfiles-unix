[[ -d $HOME/.nodenv ]] || return 0

export NODENV_ROOT=$HOME/.nodenv
export NODE_VERSIONS=$NODENV_ROOT/versions
export NODE_VERSION_PREFIX=
export PATH=$NODENV_ROOT/bin:$PATH
eval "$(nodenv init -)"

# vi:ft=sh

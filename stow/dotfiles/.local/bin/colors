#!/usr/bin/env sh

# Create fg and bg color value arrays to be sourced.

# already sourced
test "$reset" && exit 0

reset=`tput sgr0`

declare -ag fg bg
for n in {0..15}; do
    fg+=(`tput setaf $n`)
    bg+=(`tput setab $n`)
done

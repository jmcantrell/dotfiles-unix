#!/usr/bin/env bash

dir=${NVM_DIR:-$HOME/.nvm}

if [[ -d $dir ]]; then
	cd "$dir"
	git fetch --tags origin
else
	git clone https://github.com/nvm-sh/nvm.git "$dir"
	cd "$dir"
fi

git checkout "$(git latest-tag)"

. nvm.sh 

nvm install node

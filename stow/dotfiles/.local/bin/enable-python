#!/usr/bin/env sh

# Install global python packages.

set -e

pip_install() {
    pip install --upgrade --upgrade-strategy only-if-needed "$@"
}

pip_install pip wheel
pip_install poetry virtualenv black bpython pudb i3ipc jedi-language-server neovim

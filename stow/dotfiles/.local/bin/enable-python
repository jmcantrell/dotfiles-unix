#!/usr/bin/env sh

# Install global python packages.

set -e

pip_install() {
    pip install --upgrade --upgrade-strategy only-if-needed "$@"
}

pip_install pip
pip_install wheel poetry virtualenv
pip_install black bpython pudb i3ipc jedi-language-server neovim
